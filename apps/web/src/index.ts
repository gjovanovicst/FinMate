/**
 * Angular client (PWA-first, responsive desktop + mobile).
 *
 * Owner: docs/07-platform-strategy-mobile-desktop.md
 *
 * Phase 0 task 0.8 scaffolds the real Angular application (routing, layout shell, design tokens,
 * `<ui-money>`). This file exists now only so the workspace and the boundary rule have a `scope:web`
 * project; it is NOT the Angular entry point and will be replaced.
 */
export {};
