{
  "name": "@finmate/config",
  "version": "0.0.0",
  "private": true,
  "description": "Shared tooling config: tsconfig, eslint and prettier presets.",
  "main": "src/index.ts",
  "types": "src/index.ts",
  "scripts": {
    "lint": "eslint .",
    "typecheck": "tsc --noEmit -p tsconfig.json"
  }
}
